<?xml version="1.0" encoding="utf-8"?>
<!--
  AndroidManifest.xml for TuneLab
  Place this at: android/app/src/main/AndroidManifest.xml
  after running: npx cap add android

  PERMISSIONS — only what we actually use:
    INTERNET          → AI provider API calls
    VIBRATE           → haptic feedback on buttons
    ACCESS_NETWORK_STATE → check connectivity before AI calls
    POST_NOTIFICATIONS → optional: update notifications when FH6 car list updates
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <!-- ── Permissions ── -->
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.VIBRATE" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

  <!-- Optional: notify users when FH6 car list is updated -->
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

  <!-- ── Hardware features (not required — don't block installs) ── -->
  <uses-feature android:name="android.hardware.touchscreen"
                android:required="false" />
  <uses-feature android:name="android.hardware.wifi"
                android:required="false" />

  <application
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:theme="@style/AppTheme"
      android:supportsRtl="true"
      android:hardwareAccelerated="true"
      android:usesCleartextTraffic="false"
      android:networkSecurityConfig="@xml/network_security_config"

      <!-- DeX / large screen support -->
      android:resizeableActivity="true">

    <activity
        android:name=".MainActivity"
        android:exported="true"
        android:launchMode="singleTask"

        <!-- Edge-to-edge: let app draw under status and nav bars -->
        android:windowSoftInputMode="adjustResize"

        <!-- 120Hz / high refresh rate support -->
        android:preferMinimalPostProcessing="true"

        <!-- DeX: allow free-form window resizing -->
        android:resizeableActivity="true"
        android:supportsPictureInPicture="false"

        android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode">

      <!-- Main launcher intent -->
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>

      <!-- Deep link: open tune JSON files shared from other apps -->
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:mimeType="application/json" />
      </intent-filter>

      <!-- Share target: receive text shared from other apps (tune codes) -->
      <intent-filter>
        <action android:name="android.intent.action.SEND" />
        <category android:name="android.intent.category.DEFAULT" />
        <data android:mimeType="text/plain" />
      </intent-filter>

    </activity>

    <!-- Capacitor file provider for exports -->
    <provider
        android:name="androidx.core.content.FileProvider"
        android:authorities="${applicationId}.fileprovider"
        android:exported="false"
        android:grantUriPermissions="true">
      <meta-data
          android:name="android.support.FILE_PROVIDER_PATHS"
          android:resource="@xml/file_paths" />
    </provider>

  </application>

</manifest>
